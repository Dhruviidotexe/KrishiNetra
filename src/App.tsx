import React from 'react';
// Use the original component file at the project root so no components are changed or removed
import OilseedMarketplace from '../oilseed-marketplace';

export default function App() {
  return <OilseedMarketplace />;
}
